# SQLite database file. Path is resolved relative to the repo root, so it is
# consistent between the Prisma CLI (migrate/studio) and the running server.
DATABASE_URL="file:./prisma/dev.db"

# HTTP port for the Fastify server
PORT=4000
